"import React, { useEffect, useState } from 'react';\nimport { motion, AnimatePresence } from 'framer-motion';\n\nexport const Preloader = () => {\n  const [isLoading, setIsLoading] = useState(true);\n\n  useEffect(() => {\n    // Hide scrollbar while preloader is active\n    document.body.style.overflow = 'hidden';\n    \n    // The Nolan Hold: Tense, dead silence for exactly 1.6 seconds.\n    const timer = setTimeout(() => {\n      setIsLoading(false);\n      document.body.style.overflow = '';\n    }, 1600);\n\n    return () => {\n      clearTimeout(timer);\n      document.body.style.overflow = '';\n    };\n  }, []);\n\n  return (\n    <AnimatePresence>\n      {isLoading && (\n        <motion.div\n          // Hard cut in (no fade). Smash cut out (0.1s snap to zero opacity).\n          initial={{ opacity: 1 }}\n          exit={{ opacity: 0 }}\n          transition={{ duration: 0.1, ease: \"linear\" }}\n          style={{\n            position: 'fixed',\n            inset: 0,\n            backgroundColor: '#000000', // Pure cinematic pitch black\n            zIndex: 99999, // Ensure it covers absolutely everything\n            display: 'flex',\n            alignItems: 'center',\n            justifyContent: 'center',\n            color: '#ffffff', // Stark white\n          }}\n        >\n          {/* Mechanical Title Slam */}\n          <motion.div\n            initial={{ scale: 1.05, letterSpacing: '24px', filter: 'blur(8px)' }}\n            animate={{ scale: 1, letterSpacing: '8px', filter: 'blur(0px)' }}\n            transition={{ duration: 0.8, ease: \"easeOut\" }}\n            style={{\n              fontFamily: \"'Dune Rise', var(--font-system)\", \n              fontSize: 'clamp(32px, 7vw, 100px)', \n              display: 'flex',\n              alignItems: 'center',\n              textShadow: '0 0 40px rgba(255,255,255,0.1)'\n            }}\n          >\n            SANDST<span style={{ color: 'var(--accent-color)', transform: 'scale(1.15)', margin: '0 8px', textShadow: '0 0 20px var(--accent-color)' }}>
<truncated 106 bytes>