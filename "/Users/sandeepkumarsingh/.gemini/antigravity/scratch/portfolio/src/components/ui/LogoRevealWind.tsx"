"import React, { useEffect, useRef } from 'react';\nimport { gsap } from 'gsap';\nimport { ScrollTrigger } from 'gsap/ScrollTrigger';\n\ngsap.registerPlugin(ScrollTrigger);\n\nconst FINAL_CHARS = ['S','A','N','D','S','T','o','R','M','I','F','Y'];\n\nexport const LogoRevealWind = () => {\n  const containerRef = useRef<HTMLDivElement>(null);\n  const charRefs = useRef<(HTMLSpanElement | null)[]>([]);\n\n  useEffect(() => {\n    if (!containerRef.current) return;\n\n    const ctx = gsap.context(() => {\n      const tl = gsap.timeline({\n        scrollTrigger: {\n          trigger: containerRef.current,\n          start: 'top top',\n          end: '+=100%',\n          scrub: 1, // Smooth physical scrubbing\n          pin: true,\n          anticipatePin: 1\n        }\n      });\n\n      // Initially set all characters completely off-screen to the right with extreme skew and blur\n      gsap.set(charRefs.current, {\n        x: '200vw',\n        skewX: -45,\n        filter: 'blur(20px)',\n        opacity: 0,\n        scaleX: 1.5 // stretch out\n      });\n\n      // Animate them slamming into place, staggered from left to right\n      tl.to(charRefs.current, {\n        x: 0,\n        skewX: 0,\n        filter: 'blur(0px)',\n        opacity: 1,\n        scaleX: 1,\n        duration: 2,\n        ease: 'elastic.out(1, 0.7)', // Gives it a massive heavy impact bounce at the end\n        stagger: 0.1 // They fly in one by one very fast\n      });\n\n    }, containerRef);\n\n    return () => ctx.revert();\n  }, []);\n\n  return (\n    <section \n      id=\"branding-wind\" \n      ref={containerRef} \n      style={{ \n        minHeight: '100vh', \n        display: 'flex', \n        alignItems: 'center', \n        justifyContent: 'center', \n        background: 'var(--bg-color)',\n        overflow: 'hidden',\n        position: 'relative'\n      }}\n    >\n      <div className=\"container\" style={{ textAlign: 'center', position: 'relative', zIndex: 10 }}>\n        \n        <h2 className=\"text-headline\" style={{ fontSize: '18
<truncated 1332 bytes>