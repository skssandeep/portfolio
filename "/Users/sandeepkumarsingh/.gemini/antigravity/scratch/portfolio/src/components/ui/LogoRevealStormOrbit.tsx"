"import React, { useLayoutEffect, useRef } from 'react';\nimport { gsap } from 'gsap';\nimport { ScrollTrigger } from 'gsap/ScrollTrigger';\n\ngsap.registerPlugin(ScrollTrigger);\n\nconst LEFT_CHARS = ['S','A','N','D','S','T'];\nconst RIGHT_CHARS = ['R','M','I','F','Y'];\n\nexport const LogoRevealStormOrbit = () => {\n  const containerRef = useRef<HTMLDivElement>(null);\n  const wrapperRef = useRef<HTMLDivElement>(null);\n  const charRefs = useRef<(HTMLSpanElement | null)[]>([]);\n  const eyeRef = useRef<HTMLDivElement>(null);\n  const pupilRef = useRef<HTMLDivElement>(null);\n\n  useLayoutEffect(() => {\n    if (!containerRef.current || !wrapperRef.current || !eyeRef.current) return;\n\n    const ctx = gsap.context(() => {\n      // 1. Calculate transform origins for each letter so they locally orbit the exact center of the eye\n      const eyeRect = eyeRef.current!.getBoundingClientRect();\n      const eyeCenterX = eyeRect.left + eyeRect.width / 2;\n      const eyeCenterY = eyeRect.top + eyeRect.height / 2;\n\n      charRefs.current.forEach((char) => {\n        if (!char) return;\n        const charRect = char.getBoundingClientRect();\n        \n        const originX = eyeCenterX - charRect.left;\n        const originY = eyeCenterY - charRect.top;\n\n        // Letters start in chaotic, multi-rotational local orbits\n        gsap.set(char, {\n          transformOrigin: `${originX}px ${originY}px`,\n          rotation: -1440 + (Math.random() * 720), // Wild local orbits\n          opacity: 0,\n        });\n      });\n\n      // 2. Set the initial state of the massive storm wrapper\n      gsap.set(wrapperRef.current, {\n        scale: 15, // The storm is MASSIVE, eye fills the screen\n        rotation: -720, // The entire storm system revolves globally\n      });\n\n      // 3. Set the initial shape of the eye\n      gsap.set(eyeRef.current, {\n        borderRadius: '75% 0%',\n        rotation: 45,\n      });\n\n      // 4. Create the timeline\n      const tl = gsap.timeline({\n        scrollTrigger: {\n         
<truncated 4349 bytes>