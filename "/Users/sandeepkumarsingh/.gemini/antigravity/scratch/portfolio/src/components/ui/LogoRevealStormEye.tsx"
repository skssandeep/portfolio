"import React, { useLayoutEffect, useRef } from 'react';\nimport { gsap } from 'gsap';\nimport { ScrollTrigger } from 'gsap/ScrollTrigger';\n\ngsap.registerPlugin(ScrollTrigger);\n\nconst LEFT_CHARS = ['S','A','N','D','S','T'];\nconst RIGHT_CHARS = ['R','M','I','F','Y'];\n\nexport const LogoRevealStormEye = () => {\n  const containerRef = useRef<HTMLDivElement>(null);\n  const charRefs = useRef<(HTMLSpanElement | null)[]>([]);\n  const eyeRef = useRef<HTMLDivElement>(null);\n  const pupilRef = useRef<HTMLDivElement>(null);\n\n  useLayoutEffect(() => {\n    if (!containerRef.current || !eyeRef.current) return;\n\n    const ctx = gsap.context(() => {\n      // Set the initial state: Eye is massive, centered, and spinning like a hurricane.\n      // Other letters are invisible and scattered far away.\n      \n      gsap.set(eyeRef.current, {\n        scale: 20, // Massive storm eye\n        rotation: -720, // Huge spin\n        borderRadius: '50% 50%', // Starts as a perfect circle (the eye of the storm)\n        boxShadow: '0 0 100px rgba(239,68,68,1)',\n      });\n\n      gsap.set(pupilRef.current, {\n        scale: 3, // Dilated pupil\n        opacity: 0.5\n      });\n\n      charRefs.current.forEach(char => {\n        if (!char) return;\n        // Letters are caught in the outer bands of the storm\n        gsap.set(char, {\n          x: (Math.random() - 0.5) * window.innerWidth * 2,\n          y: (Math.random() - 0.5) * window.innerHeight * 2,\n          rotation: Math.random() * 720 - 360,\n          scale: 0,\n          opacity: 0,\n        });\n      });\n\n      const tl = gsap.timeline({\n        scrollTrigger: {\n          trigger: containerRef.current,\n          start: 'top top',\n          end: '+=200%', // Longer pin for a dramatic storm dissipation\n          scrub: 1.2,\n          pin: true\n        }\n      });\n\n      // 1. The Eye rapidly shrinks and spins down to its normal size\n      tl.to(eyeRef.current, {\n        scale: 1,\n        rotation: 0,\n        borderRadius: '50% 50%',\n        bo
<truncated 3807 bytes>